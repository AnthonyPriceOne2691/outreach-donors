# Active delivery status

- **slug:** failure-outcomes (отказ провайдера называется своим именем)
- **stack:** delivery@1.93, cqg@deployed, okf@0.3
- **class:** S
- **kind:** fix
- **phase:** build
- **builder:** agent:claude
- **verifier:** human:anthony
- **human_ok_spec:** yes at=2026-09-22 by=human:anthony («что у нас
  с обработкой ошибок, все отказы должны обрабатываться корректно
  и показывать информацию»)
- **human_ok_plan:** yes at=2026-09-22 by=human:anthony (там же: «понять
  что собранные цепочки работают, как они работают и какие есть дыры
  и слепые пятна»)

Предыдущий срез `stage5-template` закрыт, его статус —
в `delivery/archive/stage5-template.md`.

## Откуда взялся срез

Из несостоявшегося замера. Собирались мерить, что даёт Hunter при
`type=generic&limit=3` — правка на одну строку, двигающая тариф
на $150/мес. Все 88 запросов отбиты: **учётка Hunter ограничена**.
Замер заблокирован снаружи, а на его месте нашлось то, что важнее.

## Что в срезе

**Три границы с внешними сервисами перестали врать про свои отказы.**
Правило целиком — `okf/provider-refusals.md`.

**1. Hunter.** `restricted_account` приезжает с HTTP 429 и был неотличим
от «слишком часто». Разбор теперь идёт по маркеру, а не по коду: таблица
`_MARKERS`, отдельное исключение `ProviderBlockedError`, отдельный исход
`ContactStatus.BLOCKED` и красная подпись «учётка закрыта» на экране —
красная, а не жёлтая, потому что жёлтое оператор ждёт, а здесь ждать
нечего. Незнакомый маркер больше не подводится под знакомый.

**2. Слой модели.** `post_chat` возвращал `None` на всё — сеть, 401, 500,
неразобранный JSON. Теперь возвращает `Refusal`: вид, текст провайдера
и признак «повтор не поможет». Замысел «отказ не рушит вызывающего»
сохранён, сломано было не это, а то, что причина уезжала только в лог.
Текст отказа берётся из `error.message` провайдера — он прямо говорит,
что чинить.

**3. Генерация ключей.** `None` превращался в `[]`, и отчёт писал «модель
исчерпала уникальные фразы» про неработающий ключ. Теперь отказы —
отдельное число в отчёте; пустой пул при непустом списке отказов роняет
прогон **до траты на выдачу**; пустой пул без отказов остаётся законным
исходом. Неисправимый отказ прекращает звонки: было двенадцать обречённых
вызовов, стало один.

**Попутно:** ключ с не-ASCII символами валил `UnicodeEncodeError` мимо
всей обработки — заголовки только ASCII, тот же класс, что у секрета
приёма ответов.

## Оракулы

- **shape-oracles:** cqg-deployed — гейты, ратчет, eslint, prettier, tsc
- **behavior-oracles:** tests-present — 6 новых проверок: закрытая учётка
  не выдаётся за превышенную частоту; незнакомый маркер не выдаётся
  за временный; пустой пул из-за отказов роняет прогон; пустой пул без
  отказов остаётся результатом; отказы считаются в отчёте; неисправимый
  отказ прекращает вызовы
- **ci-oracles:** deployed — `check`, `web`, `docker`
- **artifact_oracle:** живой отказ Hunter и живой 401 OpenAI
- **irreversible_surfaces:** прежние, срез новых не добавляет
- **model_surface:** вызывается — живой 401 против настоящего OpenAI,
  фраз не запрашивалось, токенов не потрачено
- **rule_enforcers:** маркер разбирается до кода ответа; незнакомый маркер
  не классифицируется догадкой; причина отказа едет со значением, а не
  только в лог; неисправимый отказ не повторяется

## Проверка вторым способом

**Оба дефекта проверены живьём, а не заглушками** — заглушка отвечала бы
так, как её научили.

- Hunter: тот же живой отказ после правки называется
  `ProviderBlockedError` с текстом «зайти в кабинет». Кредитов
  потрачено ноль в обоих прогонах.
- Модель: негодный ключ против настоящего OpenAI. Прогон останавливается
  до траты на выдачу, причина — дословная фраза провайдера со ссылкой
  на страницу ключей. Двенадцать отказов в отчёте, один вызов по сети.
- Кириллица в ключе: отдельный прогон, отказ назван, трассировки нет.

Миграция перечисления прогнана на живой базе, не только в тесте.

## Чего в срезе нет

**Замер Hunter `type=generic` не сделан** — учётка закрыта. Вопрос
о тарифе Growth против Scale ($150/мес) остаётся открытым до того,
как Anthony зайдёт в кабинет.

**У Ahrefs 429 не разделён** на «занят» и «юниты кончились». Не тронуто
намеренно: проверить это можно только исчерпав квоту, а вслепую я
переписал бы рабочую классификацию по догадке — ровно то, против чего
этот срез.

DataForSEO и SendGrid разбирают отказ по телу и выглядят здоровыми,
но живым отказом не проверялись.
