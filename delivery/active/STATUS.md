# Active delivery status

- **slug:** contact-ladder + complexity-ratchet
- **stack:** delivery@1.88, cqg@deployed, okf@0.3
- **class:** M
- **kind:** feature
- **phase:** verify
- **builder:** agent:claude
- **verifier:** human:anthony
- **human_ok_spec:** yes at=2026-09-18 by=human:anthony («Ф4.1 — лестница контактов»)
- **human_ok_plan:** yes at=2026-09-18 by=human:anthony (PHASES.md, Ф4; цепочка согласована ранее)

Предыдущий срез `run-pipeline-persistence` закрыт: Ф3 принята, код в `main`.

## Что в срезе

Ступени поиска контакта от бесплатных к платной и сохранение исхода:

    MX → страницы сайта → RDAP → платный сервис → форма в ручную очередь

Отправка писем в срез **не входит**: она ждёт шаблон письма и двадцать
почтовых доменов. Лестница от этого не зависит и делается заранее, потому что с неё
начинается календарь Ф4.

## Оракулы

- **shape-oracles:** cqg-deployed — шесть архитектурных гейтов (шестой,
  `secret-in-example`, роняет пуш при заполненном секрете в образце окружения)
  плюс ратчет сложности: снимок по каждому файлу, расхождение в любую
  сторону — отказ
- **behavior-oracles:** tests-present — 260 тестов; логика без сети, репозиторий
  на настоящей базе, маршрут лестницы — по счёту вызовов
- **ci-oracles:** deployed — .github/workflows/ci.yml на каждый push и PR
- **artifact_oracle:** n/a reason=выход среза — записи в базе и отчёт в консоли
- **model_surface:** n/a reason=модель по-прежнему не вызывается; В3 придёт
  с уникализацией письма
- **rule_enforcers:** n/a reason=model_surface не объявлена

## Проверка вторым способом

Тесты зелёные — это один угол зрения. Второй дал три находки, которых
заглушки не показали:

1. **Ступень 0 не работала вовсе.** В системной настройке машины первыми
   стоят IPv6-резолверы без маршрута; каждый запрос истекал по таймауту,
   ступень возвращала «неизвестно» по всем доменам подряд и при этом стоила
   пяти секунд на домен. Выглядело как работающая проверка. Починено
   запасными резолверами и памятью об отказе; доля «неизвестно» теперь
   отдельное число в отчёте — именно её отсутствие делало поломку невидимой.
2. **Деобфускация даёт правдоподобный мусор.** Фраза «meet at the dot com»
   после замены выглядит как `meet@the.com`, и фильтру качества придраться
   не к чему. Угаданные адреса отделены от прямых и принимаются только на
   домене сайта или на известной бесплатной почте.
3. **Мера сложности считала чужой код своим.** Ветвления вложенной функции
   приписывались внешней — и дважды, потому что у вложенной есть собственная
   запись. Нашёл свой же тест на вложенную функцию, написанный до того, как
   стало «и так понятно». Обход заменён на свой, лямбды при этом считаются
   вместе с хозяином: отдельной записи у них нет.
4. **RDAP на `.com` не дал ничего.** Пять доменов — ноль адресов, два
   таймаута. Ступень оставлена (бесплатна), таймаут снижен вдвое, судьба
   решается по счётчику на боевом объёме.

Живой прогон после правок: пять доменов из шести с адресом, три закрыты
бесплатными ступенями, платная вошла дважды — ровно та экономика, ради
которой выбран порядок.

## Волны контура

| Волна | Ось | Состояние | Причина |
|---|---|---|---|
| **В0** | процесс | развёрнута 18.09.2026 | — |
| **В1** | гейты вида + мерж | **развёрнута полностью 18.09.2026** | добавлены гейт `secret-in-example` и ратчет сложности со снимком. Долг закрыт: раньше не хватало именно снимка |
| **В2** | канон домена | развёрнута 18.09.2026, дополнена | восьмое понятие — `contact-ladder` |
| **В3** | поведение модели | не пришла | вызовов модели в коде нет; придёт с уникализацией письма |

## Долг, признанный явно

- **Доля прохождения воронки не откалибрована.** Остатка квоты на калибровку
  хватает с запасом — проверено бесплатным запросом остатка. Осталось
  получить сотню боевых ключевых слов. Понятие `okf/funnel-calibration`
  открыто.
- **Ступень 2 (RDAP) под вопросом** — см. выше. Снимать или оставлять,
  решит счётчик `rdap_found` на боевом объёме.
- **Доля невалидных адресов не измерена.** Проверка адреса у платного
  сервиса в тарифе есть, но в лестницу не подключена:
  до первой отправки цена ошибки неизвестна, после — измеряется отказами
  доставки. Подключать, когда появятся домены и первые отправки.

## Прочее

- **blockers:** шаблон письма и почтовые домены — блокируют отправку, не лестницу;
  учётка DataForSEO (адаптер, не фаза)
- **new_dependency:** dnspython — запись MX. В стандартной библиотеке запроса
  к DNS нет, а без ступени 0 письма уходят на домены, которые их не принимают
- **runtime_paths:** db — отбор доноров без контакта и запись исхода пачками;
  сеть — чужие сайты, RDAP, платный сервис
- **waivers:** none
