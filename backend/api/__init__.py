"""Веб-слой: маршруты, схемы запросов, зависимости.

Здесь и только здесь живёт FastAPI — гейт `layers` проверяет это
механически. Правил тут нет: обработчик передаёт ядру то, что нажал
человек, и показывает то, что ядро решило.
"""
